name: routeros_api
description: A robust Dart package for MikroTik RouterOS API. Supports persistent connections, heartbeats, auto-reconnect, and real-time streaming.
version: 0.1.2
repository: https://github.com/ginkohub/routeros_api
homepage: https://github.com/ginkohub/routeros_api
issue_tracker: https://github.com/ginkohub/routeros_api/issues

topics:
  - mikrotik
  - routeros
  - networking
  - api-client
  - hotspot

environment:
  sdk: ^3.0.0

dependencies:
  # No external dependencies for core functionality

dev_dependencies:
  lints: ^2.1.0
  test: ^1.24.0
